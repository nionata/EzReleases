name: 'EzTag'
description: 'Automatic Semantic Versioning for your Repo'
inputs:
  repo_token:
    required: true
    description: 'The GITHUB_TOKEN secret'
runs:
  using: 'node12'
  main: 'src/build/index.js'